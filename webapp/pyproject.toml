[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = [
    "E501",  # line too long — handled by black
]
per-file-ignores = { "apps/jobs/migrations/*.py" = ["E501"] }
